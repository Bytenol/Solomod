export default function CodeFilterComp() {
    return (
    <div>
        <select >Filter:
            <option value={"HotToday"} />
            <option value={"Most recents"} />
        </select>
    </div>
    );
}